{
  "extends": "./tsconfig.base.json",
  "include": ["**/*.ts", "**/*.tsx", ".storybook/**/*.ts"],
  "exclude": ["node_modules", ".wrangler", ".vercel", ".next", "storybook-static", "tmp"],
  "compilerOptions": {
    "types": ["vitest/globals"]
  }
}
